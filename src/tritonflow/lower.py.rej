--- lower.py
+++ lower.py
@@ -341,6 +341,8 @@
         try:
             emu_result = emulate(program, emu_inputs, policy=policy)
         except Exception as exc:
+            import traceback
+            traceback.print_exc()
             exec_err = str(exc)
 
         if emu_result is not None:
